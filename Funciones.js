// En Construcción.
// Arreglar a lo que se pidió (Nada)

const products= {
    nombre: 'Macs',
    precio: 1.2,
    disponible: true,
    mostrarInfo: function() {
        console.log(`El producto: ${this.nombre} tiene un precio de: ${this.precio}`)
    }

}


producto.mostrarInfo();